{
  "name": "air-quality-api",
  "version": "1.0.0",
  "description": "",
  "main": "./build/server.js",
  "scripts": {
    "predev": "npm run clean && npm run build",
    "prestart": "npm run clean && npm run build",
    "dev": "nodemon --watch build/server.js",
    "test": "jest --watchAll",
    "build": "npx tsc --build",
    "clean": "npx tsc --build --clean",
    "tsc": "npx tsc -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^20.1.3",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  }
}
